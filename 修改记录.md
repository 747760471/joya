# Joya语言开发修改记录

## 阶段九：LLVM 后端 Phase 4.0（基础设施搭建）
### 日期：2026年5月7日

#### 1. 环境搭建
- 安装 LLVM 20.1.0（通过 winget 下载 + 静默安装）
- 从 GitHub 下载完整的 `llvm-c` C API 头文件（27 个 .h 文件）到 `D:\joya\llvm-c\`
- 手动创建 `llvm-config.h`（Windows x86_64 配置）
- 在 `build.zig` 中配置 LLVM-C.lib 链接

#### 2. 编译器架构 (`src/compiler.zig`)
- **手动声明 extern 函数**：不使用 `@cImport`（避免解析 4800 行 Core.h 的兼容性问题），手动声明约 80 个需要的 LLVM C API 函数
- **类型系统**：Joya 类型 → LLVM 类型映射
  - `int` → `i64`, `float` → `f64`, `bool` → `i1`
  - `string` → `i8*`, `void` → `void`
  - `chan/array/map/class_ref/type_param` → `i8*`（opaque pointer）
- **方法编译**：
  - 方法名 mangle：`ClassName_method`
  - 静态方法 → LLVM function（无 `this` 参数）
  - 实例方法 → 第一个参数为 `this`（i8*）
  - `main` 方法自动识别为静态方法
- **变量管理**：`alloca` + `store`/`load` 模式，变量类型通过 `variable_types` HashMap 跟踪
- **内存管理**：ArenaAllocator 管理编译器临时分配，零泄漏

#### 3. 支持的编译功能
| 功能 | 状态 | 说明 |
|------|------|------|
| 变量声明 + 赋值 | ✅ | alloca + store/load |
| 整数/浮点/布尔字面量 | ✅ | LLVMConstInt/ConstReal |
| 字符串字面量 | ✅ | LLVMBuildGlobalString + bitcast |
| 算术运算 (+,-,*,/,%) | ✅ | Add/Sub/Mul/SDiv/SRem |
| 比较运算 (<,>,<=,>=,==,!=) | ✅ | ICmp (SLT/SGT/SLE/SGE/EQ/NE) |
| 逻辑运算 (&&,\|\|) | ✅ | 短路求值 + Phi 节点 |
| 一元运算 (!) | ✅ | LLVMBuildNot |
| if/else | ✅ | CondBr + then/else/merge |
| while 循环 | ✅ | CondBr + cond/body/end |
| for 循环 | ✅ | init/cond/body/inc/end |
| return | ✅ | Ret/RetVoid + 隐式返回 |
| print/println | ✅ | printf 格式化输出 |
| 方法调用 | ✅ | Call2 + mangle 查找 |
| 类型感知加载 | ✅ | variable_types HashMap |

#### 4. CLI 新增参数
```
joya <file.joya> [--compile <output.o>] [--emit-ir <output.ll>] [--target <triple>]
```
- `--compile`：编译为目标文件（.o），再用 `zig cc` 链接为可执行文件
- `--emit-ir`：输出 LLVM IR 文本（.ll），用于调试
- `--target`：指定目标三元组（默认本机）

#### 5. C 运行时 (`src/joya_runtime.c`)
- `main()` 入口点：调用 `Hello_main()` 并返回结果
- `__chkstk`：Windows 栈探测函数（alloca 生成的代码需要）
- 链接命令：`zig cc output.o src/joya_runtime.c -o output.exe`

#### 6. 文件结构变化
```
新增文件：
  src/compiler.zig      — LLVM 后端编译器（~1140 行）
  src/joya_runtime.c    — C 运行时入口
  llvm-c/               — LLVM C API 头文件（27 个 .h）
  examples/compiler_test.joya — 编译器测试用例

修改文件：
  src/main.zig          — 添加 --compile / --emit-ir / --target 参数
  build.zig             — 添加 LLVM-C.lib 链接配置
```

#### 7. 验证结果
- 编译器测试程序：int 算术 + if/else + while 循环 + print
- 输出：`30\n100\n10\n` ✅（与预期一致）
- 解释器模式未受影响 ✅
- 内存零泄漏 ✅

---

## 阶段九（续）：LLVM 后端 Phase 4.4 — 类与对象
### 日期：2026年5月7日

#### 1. 类类型系统
- `declareClassTypes()`：为每个类创建 LLVM 命名结构体类型（如 `%Point = type { i64, i64 }`）
- `class_struct_types`：class_name → LLVM struct body type 映射
- `class_field_indices`：class_name → (field_name → struct index) 映射
- **LLVM 20 适配**：所有对象指针使用 opaque `ptr`（不使用 typed pointer），struct type 仅用于 GEP 操作

#### 2. new ClassName() 编译
- `malloc(sizeof(struct))` 分配堆内存
- 零初始化所有字段（GEP + store 0）
- 查找并调用构造函数 `ClassName_ClassName(this, args...)`
- 构造函数通过 `LLVMGlobalGetValueType` 获取函数类型

#### 3. 字段访问与赋值
- `this.field` → `GEP(struct_type, this_val, 0, field_idx)` + `load`
- `this.field = val` → `GEP(struct_type, this_val, 0, field_idx)` + `store`
- `obj.field` → 编译对象表达式 + GEP + load（通过 `current_class_name` 和字段名查找对应类）
- `current_class_name` 跟踪当前正在编译的方法所属类

#### 4. 实例方法调用
- `obj.method(args)` → 编译对象表达式，传递 `this` 作为第一个参数
- 静态方法调用：`ClassName.method(args)`（不传 `this`）
- 方法查找：先检查当前类，再搜索所有类

#### 5. 关键 LLVM 20 修复
- 将 `LLVMGetCalledFunctionType` 全部替换为 `LLVMGlobalGetValueType`（前者用于 Call 指令，后者用于 Global/function 值）
- 新增 extern 声明：`LLVMGlobalGetValueType`, `LLVMPointerType`, `LLVMCountParamTypes`, `LLVMGetParamTypes`, `LLVMGetReturnType`
- `LLVMGetParamTypes` 参数类型修正：应为 `LLVMTypeRef`（函数类型），非 `LLVMValueRef`

#### 6. 新增支持
| 功能 | 状态 | 说明 |
|------|------|------|
| 类结构体类型声明 | ✅ | LLVMStructCreateNamed + SetBody |
| new ClassName() | ✅ | malloc + 零初始化 + 构造函数调用 |
| this.field 读取 | ✅ | GEP + load |
| this.field 赋值 | ✅ | GEP + store |
| obj.field 读取 | ✅ | GEP + load（字段名查找类） |
| obj.method(args) | ✅ | 传递 this 的实例方法调用 |
| ClassName.method(args) | ✅ | 静态方法调用（不传 this） |

#### 7. 测试结果
- `examples/class_test.joya`：Point 类（2 个 int 字段 + 构造函数 + 3 个实例方法）
- 输出：`10\n20\n30\n100\n300\n` ✅
- 原有 `compiler_test.joya` 回归测试通过 ✅

---

## 阶段八：闭包/Lambda（Phase 3）
### 日期：2026年4月30日

#### 1. Lambda 表达式
- 语法：`fn(Type name, ...) { body }`
- 支持 IIFE（立即调用）：`fn(int x) { return x + 1; }(42)`
- 支持 postfix 链式调用：lambda 后可跟 `(args)` 直接调用

#### 2. 闭包变量
- 类型声明：`fn name = fn(Type p) { body };`
- 方法参数中传递：`void apply(fn func, int val) { func(val); }`
- 作为返回值：`fn makeAdder(int x) { return fn(int y) { return x + y; }; }`
- 环境捕获：值捕获（创建时克隆环境）

#### 3. 数组高阶方法（4 个）
| 方法 | 签名 | 说明 |
|------|------|------|
| `map` | `arr.map(fn)` → array | 对每个元素应用闭包，返回新数组 |
| `filter` | `arr.filter(fn)` → array | 保留闭包返回 true 的元素 |
| `forEach` | `arr.forEach(fn)` → void | 对每个元素执行闭包 |
| `reduce` | `arr.reduce(fn, init)` → value | 累积计算，fn(acc, elem) |

#### 4. 内部实现
- `Closure` 结构体：params + body + captured_env
- `Value.closure` 变体：运行时闭包值
- `evalClosureCall` / `callClosureWithArgs`：闭包调用辅助函数
- `RuntimeCtx.trackClosure`：闭包生命周期追踪和清理
- `parsePostfix` 支持 `(args)` 直接调用语法（method name 为空字符串标识）
- `method_call` 分派重构：闭包变量优先于静态方法查找
- `parseName` 扩展：`map`/`fn` 关键字可用作方法名

#### 5. AST 变更
- `Type.fn_ref`：fn 类型（用于变量声明和参数类型）
- `Expression.lambda`：`{ params: []Param, body: *Statement }`
- `body` 使用 `*Statement` 避免 Statement↔Expression 循环依赖

#### 6. 测试文件
- `examples/test_closure.joya`：9 个测试用例全部通过

#### 7. 引用捕获（Reference Capture）增强
- `RefCell` 结构体：共享可变间接层，多环境持有同一 `*RefCell` 指针
- `Value.ref_cell` 变体：运行时引用单元
- `Environment` 修改：
  - `set()` 自动写回：如果变量绑定是 `ref_cell`，更新 `cell.value` 而非覆盖绑定
  - `get()` 自动解引用：如果变量绑定是 `ref_cell`，返回 `cell.value`
  - `defineRef()` — 将变量绑定为引用（存储 `ref_cell`）
  - `getRaw()` — 返回原始 Value（可能为 `ref_cell`）
- `.lambda` 创建时：为当前环境中所有变量创建 `RefCell`，内外共享同一引用
- `callClosureWithArgs` 参数绑定：使用 `vars.put` 直接覆盖（不走 `set()` 的 ref 写回逻辑，参数是新局部绑定）
- `parseVarDecl` 扩展：`fn methodName(params) { body }` 语法支持（类型为 fn_ref + 名称 + `(` → 命名 lambda 声明）
- `RuntimeCtx.createRefCell()` + 追踪清理
- `printValue` 处理 `.ref_cell`：解引用打印内部值

#### 8. 测试文件（引用捕获）
- `examples/test_ref_capture.joya`：7 个测试用例全部通过
  - 基础引用捕获、读写共享变量、多闭包共享引用
  - 字符串引用修改、嵌套闭包、forEach 修改外部状态
  - makeCounter 工厂模式（每个闭包独立计数器）

#### 9. 泛型方法（Generic Methods）
- **语法**：`static <T> T methodName(T param)` / `static <K, V> void foo(K k, V v)`
- **类型参数**：`<T, K, V>` 在方法返回类型之前声明
- **type_param 推断**：大写短标识符（≤2字符，首字母大写）识别为类型参数 T/K/V，而非 class_ref
- **AST 变更**：
  - `Type.type_param: []const u8` — 泛型类型参数节点
  - `Method.type_params: []const []const u8` — 方法类型参数列表
- **Parser 变更**：
  - `parseMethod` 解析 `<T, V>` 类型参数列表
  - `parseBaseType` 识别大写短标识符为 `type_param`
  - `parseGenericLambdaDecl` 支持方法体内 `fn <T> T name(T x) { body }` 语法
  - `parseForLoop` for-each 检测扩展：支持 `T` 标识符作为元素类型
- **运行时**：解释器类型擦除，`type_param` 等价于"任意类型"，`defaultForType(.type_param) => null`

#### 10. 测试文件（泛型方法）
- `examples/test_generic.joya`：10 个测试用例全部通过
  - identity（int/string/bool）、max（int/float）、swap、first、contains
  - 泛型命名 lambda（doubleIt）、数组 map 组合

---

## 阶段七：语言特性充实（Phase 2.5）
### 日期：2026年4月29日

#### 1. float/string 比较运算符增强
- `==`/`!=` 支持 float/int 混合比较（`3 == 3.0` → true）
- `<`/`>`/`<=`/`>=` 支持 float/float、float/int、int/float 比较
- `<`/`>` 支持字符串字典序比较（`"abc" < "abd"` → true）

#### 2. 字符串内置方法（10 个）
| 方法 | 签名 | 说明 |
|------|------|------|
| `substring` | `s.substring(start)` / `s.substring(start, end)` | 子串截取 |
| `indexOf` | `s.indexOf(sub)` → int | 查找子串位置，-1 表示未找到 |
| `contains` | `s.contains(sub)` → bool | 是否包含子串 |
| `startsWith` | `s.startsWith(prefix)` → bool | 是否以指定前缀开头 |
| `endsWith` | `s.endsWith(suffix)` → bool | 是否以指定后缀结尾 |
| `toUpperCase` | `s.toUpperCase()` → string | 转大写 |
| `toLowerCase` | `s.toLowerCase()` → string | 转小写 |
| `trim` | `s.trim()` → string | 去除首尾空白 |
| `replace` | `s.replace(old, new)` → string | 全局替换 |
| `split` | `s.split(delim)` → string[] | 按分隔符拆分为数组 |

#### 3. 数组内置方法（8 个）
| 方法 | 签名 | 说明 |
|------|------|------|
| `push` | `arr.push(val)` → int | 追加元素，返回新长度 |
| `pop` | `arr.pop()` → value | 弹出末尾元素 |
| `contains` | `arr.contains(val)` → bool | 是否包含元素 |
| `indexOf` | `arr.indexOf(val)` → int | 查找元素位置 |
| `join` | `arr.join(sep)` → string | 用分隔符连接为字符串 |
| `removeAt` | `arr.removeAt(index)` → value | 按索引移除元素 |
| `reverse` | `arr.reverse()` | 原地反转 |
| `sort` | `arr.sort()` | 原地升序排序（插入排序） |

#### 4. Map/字典类型
- **语法**：`map<K,V>` 类型声明，`new map<K,V>()` 创建实例
- **Lexer**：新增 `keyword_map`
- **AST**：`Type.map{ key, value }`、`Expression.new_map{ key_type, value_type }`
- **Parser**：`parseBaseType` 支持 `map<K,V>`，`parsePrimary` 支持 `new map<K,V>()`
- **Interpreter**：
  - `JoyaMap` 结构体：内部使用 `StringHashMap(Value)`，key 通过 `valueToMapKey` 转为字符串
  - 支持 int/string/bool/float 作为 key
  - `Value.map` 变体
  - `RuntimeCtx.trackMap` 生命周期管理
  - `printValue` 支持 map 打印为 `{key: value, ...}`

| 方法 | 签名 | 说明 |
|------|------|------|
| `put` | `m.put(key, value)` | 添加/更新键值对 |
| `get` | `m.get(key)` → value/null | 获取值 |
| `remove` | `m.remove(key)` → value/null | 删除键值对 |
| `containsKey` | `m.containsKey(key)` → bool | 是否包含键 |
| `size` | `m.size()` → int | 键值对数量 |
| `isEmpty` | `m.isEmpty()` → bool | 是否为空 |
| `keys` | `m.keys()` → string[] | 获取所有键 |
| `values` | `m.values()` → value[] | 获取所有值 |
| `clear` | `m.clear()` | 清空 |

#### 5. method_call 分发机制重构
- `method_call` 分支改为类型优先分发：
  1. string → `evalStringMethod`
  2. array → `evalArrayMethod`
  3. map → `evalMapMethod`
  4. object → 原有类方法查找逻辑
- 所有内置方法实现为独立函数，保持 `eval_expression` 整洁

#### 验证结果
- 新增测试文件：`test_compare.joya`、`test_string_methods.joya`、`test_map.joya`
- 全部示例通过（含 1000 协程压力测试）
- 零破坏性变更

#### 6. 错误处理（try/catch/finally/throw）
- **语法**：
  ```java
  throw "error message";
  try {
      // may throw
  } catch(string e) {
      // handle error
  } finally {
      // always runs
  }
  ```
- **设计**：
  - `throw` 抛出任意表达式值（通常为 string），通过 `error.ThrowSignal` 传播
  - `catch(string e)` 捕获异常，`e` 绑定到抛出的值
  - `finally` 可选，无论是否抛出异常都会执行
  - try 块正常完成时也执行 finally
  - try 块 ReturnSignal/BreakSignal 等其他信号先执行 finally 再传播
- **Lexer**：新增 `keyword_try`、`keyword_catch`、`keyword_finally`、`keyword_throw`
- **AST**：`Statement.throw_stmt`（携带 Expression）、`Statement.try_stmt`（try_block + catch_var + catch_block + finally_block?）
- **Parser**：`parseThrowStmt`、`parseTryStmt`
- **Interpreter**：
  - `RuntimeCtx.thrown_value` 存储抛出的值
  - `error.ThrowSignal` 与 ReturnSignal/BreakSignal/ContinueSignal 同级传播
  - 方法调用和构造函数的 catch 点自动传播 ThrowSignal（`else => return err`）
  - goroutine 中 ThrowSignal 被静默忽略（与 ReturnSignal 一致）

#### 7. 复合赋值运算符
- **语法**：`x += 5;` / `x -= 3;` / `x *= 2;` / `x /= 4;` / `x %= 3;`
- **Lexer**：新增 `plus_eq`、`minus_eq`、`star_eq`、`slash_eq`、`percent_eq` 五个 token
- **Parser**：在 `parseAssignOrExprStmt` 中脱糖处理 — `x += 5` 等价于 `x = x + 5`
- 支持字符串拼接 `s += " World"`

#### 8. 模块/import 系统
- **语法**：`import ModuleName;`（文件顶部，class 之外）
- **路径搜索**：先搜索主文件所在目录，再搜索 `lib/` 子目录
- **文件映射**：`import Math;` → 搜索 `Math.joya`
- **防循环**：`imported` HashMap 跟踪已导入模块，避免重复导入和循环
- **嵌套导入**：被导入文件中的 import 也会被处理
- **Parser**：`parse()` 循环中跳过 `import` 语句（由 main.zig 预处理）
- **main.zig 重构**：
  - 先扫描 tokens 收集 import 名称
  - 按依赖顺序加载和解析导入文件
  - 所有文件的 class 合并到一个 Program 中
  - 支持 `ClassName.joya` 放在 `lib/` 目录下的惯例

#### 9. 静态方法调用
- **语法**：`ClassName.methodName(args)` — 无需创建对象即可调用类方法
- **Interpreter**：
  - `method_call` 分支中，先检查 `mc.object` 是否为 identifier 且匹配类名
  - 匹配时调用 `evalStaticMethod`：创建无 `this` 绑定的方法环境
  - 不匹配时走原有对象方法调用逻辑
- 让 import 进来的工具类可以直接使用：`Math.max(10, 20)`

#### 验证结果
- 新增测试文件：`test_compare.joya`、`test_string_methods.joya`、`test_map.joya`、`test_error.joya`、`test_compound.joya`、`test_import.joya`
- 新增库文件：`lib/Math.joya`、`lib/Utils.joya`
- 全部示例通过（含 1000 协程压力测试）
- 已知限制：import 的文件 source 缓冲区未释放（原型阶段可接受）

---

## 阶段六：跨平台 Fiber 抽象层
### 日期：2026年4月29日

#### 1. 动机
- 阶段五的调度器仅支持 Windows（硬编码 Win32 Fiber API）
- 编译为原生代码后同样需要跨平台运行时调度器
- 将平台相关代码抽象为统一接口，为 LLVM 后端铺路

#### 2. 架构设计
- **`fiber.zig`** — 跨平台公共 API 层，comptime 选择后端：
  - `FiberHandle` — 不透明句柄（各平台不同类型）
  - `createFiber(stack_size, entry, param)` → FiberHandle
  - `switchToFiber(handle)` — 切换到目标协程
  - `deleteFiber(handle)` — 销毁协程
  - `convertThreadToFiber()` — 将 OS 线程转为协程（Windows 专有，其他平台 no-op/alloc）
  - `convertFiberToThread()` — 转回普通线程
  - `getMasterFiber()` — 获取当前线程的调度器协程句柄
- **comptime 分发**：
  ```
  Windows → fiber_windows.zig
  Linux   → fiber_ucontext.zig + fiber_ucontext.c
  macOS   → fiber_ucontext.zig + fiber_ucontext.c
  Other   → fiber_fallback.zig (stub, 编译时不会链接)
  ```

#### 3. 各后端实现
- **fiber_windows.zig**：
  - `FiberHandle = LPVOID`
  - 直接映射 Win32 API：`CreateFiber`/`SwitchToFiber`/`DeleteFiber`/`ConvertThreadToFiber`/`ConvertFiberToThread`
  - threadlocal `master_fiber` + `master_fiber_set` 跟踪调度器协程

- **fiber_ucontext.zig + fiber_ucontext.c**：
  - 使用**内联汇编**实现上下文切换（非 ucontext API），兼容 musl/glibc/交叉编译
  - 支持 x86_64（System V ABI）和 aarch64（AAPCS64）两种架构
  - C 端：`JoyaFiberContext` 结构体 = 栈指针 + 入口函数 + 参数 + 栈内存
  - `joya_switch_fiber(from, to)` — 汇编保存/恢复 callee-saved 寄存器 + 栈指针
  - `fiber_startup(fc)` — 新协程入口，调用 `fc->entry(fc->param)`
  - `fiber_initial_entry_x86_64/aarch64` — naked asm 存根，从寄存器取 fc 指针跳转
  - `JOYA_SYM` 宏处理 macOS 下划线前缀（`_fiber_startup` vs `fiber_startup`）
  - Zig 端：threadlocal `current_fiber` + `current_fiber_set` 跟踪当前活跃协程

- **fiber_fallback.zig**：
  - 全部 stub 实现，`createFiber` 返回 `error.FiberNotSupported`
  - 调度器初始化失败后自动回退到 OS 线程模式

#### 4. scheduler.zig 重构
- 移除所有 `windows`/`LPVOID`/`WINAPI`/`kernel32` 引用
- `Goroutine.fiber_handle` 类型从 `LPVOID` 改为 `fiber.FiberHandle`
- `spawn()` 使用 `fiber.createFiber(0, goroutineEntry, ctx)` 替代 `CreateFiber`
- `goroutineEntry` 使用 `callconv(.C)` 统一调用约定
- `blockCurrent()` 使用 `fiber.switchToFiber(fiber.getMasterFiber())` 替代 `SwitchToFiber(g.scheduler_fiber)`
- 移除 `Goroutine.scheduler_fiber` 字段（由 `fiber.getMasterFiber()` threadlocal 替代）
- 移除 `WorkerThread.scheduler_fiber` / `current_goroutine` 字段
- 移除 `tls_scheduler_fiber` threadlocal（不再需要）
- `workerMain` 使用 `fiber.convertThreadToFiber()` / `fiber.convertFiberToThread()`

#### 5. build.zig 条件编译
- Linux/macOS 目标时，添加 `fiber_ucontext.c` 为 C 源文件并链接 libc
- Windows 目标时，不添加 C 文件（纯 Zig + Win32 API）
- 通过 `target.result.os.tag` 在构建时判断

#### 6. interpreter.zig
- 无需修改！所有平台相关代码已通过 `scheduler` 模块间接调用
- `scheduler.tls_current_goroutine`、`scheduler.tls_scheduler`、`scheduler.yield()` 接口不变

#### 验证结果
- Windows 构建 + 全部 12 个示例通过（含 1000 协程压力测试）
- 交叉编译验证（5 个目标全部通过）：
  - `x86_64-linux` ✅
  - `aarch64-linux` ✅
  - `x86_64-macos` ✅
  - `aarch64-macos` ✅
  - `x86_64-windows`（本地）✅
- 零破坏性变更：所有原有功能完全保持

---

## 阶段五：用户态协程调度器
### 日期：2026年4月27日

#### 1. 架构设计
- **M:N 调度模型**：N 个用户态协程运行在 M 个 OS 工作线程上（M = CPU 核心数）
- **Windows Fiber API**：使用 `CreateFiber`/`SwitchToFiber`/`DeleteFiber` 实现纳秒级上下文切换
- **全局运行队列**：锁保护的可执行协程队列，工作线程竞争获取
- **Chan 集成**：阻塞时 `yield()` 让出 CPU，数据可用时 `unblock()` 重新入队

#### 2. scheduler.zig — 核心调度器实现
- **Goroutine 结构体**：fiber 句柄、scheduler_fiber（回切指针）、状态（runnable/running/blocked/done）、唯一 ID
- **Scheduler 结构体**：
  - 全局运行队列 `global_queue`
  - 全部协程追踪 `all_goroutines`（用于清理）
  - 工作线程数组 `workers`
  - `spawn(func, user_data)` 创建并入队协程
  - `enqueue`/`dequeue` 就绪队列操作
  - `blockCurrent` 阻塞当前协程（yield 回调度器）
  - `unblock` 唤醒阻塞协程（重新入队）
- **WorkerThread**：每个工作线程持有 scheduler_fiber 和 current_goroutine
- **threadlocal 变量**：`tls_current_goroutine`、`tls_scheduler_fiber`、`tls_scheduler`
- **workerMain**：ConvertThreadToFiber → 循环 dequeue → SwitchToFiber(goroutine) → goroutine 完成或 yield → 重复
- **GoroutineLaunchCtx**：传递 func + user_data 到 Fiber 入口
- **goroutineEntry**：Windows Fiber 回调入口，执行用户函数后标记 done 并 yield 回调度器

#### 3. Fiber API 声明（Zig 0.12.0 Windows）
- `CreateFiber`、`SwitchToFiber`、`DeleteFiber`
- `ConvertThreadToFiber`、`ConvertFiberToThread`
- 调用约定 `callconv(WINAPI)`，x86_64 上等价于 `.c`
- 回调类型 `PFIBER_START_ROUTINE = *const fn (?LPVOID) callconv(WINAPI) void`
- 已通过独立测试验证：`Fiber test PASSED: count=111`

#### 4. 解释器集成
- **go_stmt**：调度器模式下 `sched.spawn(goRoutineEntry, go_ctx)`，回退模式仍用 `std.Thread.spawn`
- **goRoutineEntry**：从 user_data 恢复 GoCtx（env/ctx/body），执行语句，deinit Environment，destroy GoCtx
- **run() 函数重构**：
  - 调度器模式：创建 Scheduler → main 协程 spawn → `scheduler.run()` → 清理
  - 回退模式：原有 OS 线程方式不变
  - 调度器模式下 env/ctx 生命周期由 main goroutine 负责（`env.deinit()` + `ctx.deinit()`）
- **Chan 重写**：
  - 移除 `send_cond`/`recv_cond`（condvar）
  - 新增 `send_waiters`/`recv_waiters`（等待协程列表）
  - 新增 `use_scheduler` 标志（自动检测 `tls_scheduler != null`）
  - `send` 满时：当前协程加入 send_waiters → `scheduler.yield()`
  - `receive` 空时：当前协程加入 recv_waiters → `scheduler.yield()`
  - `close`：唤醒所有等待协程

#### 5. 内存泄漏修复
- **根因**：调度器模式下 `run()` 的 `env`/`ctx` 被 main goroutine 借走后，`defer` 清理和 goroutine 内清理冲突
- **修复**：调度器模式下 env/ctx 在 MainCtx 中创建，由 main goroutine 内部 `deinit()`，`run()` 不再 defer 清理它们
- **结果**：全部 9 个测试文件 + 压力测试零泄漏

#### 6. 压力测试
- `examples/stress_test.joya`：1000 个 goroutine 各自 send 自己的 ID，主协程 receive 并求和
- 结果：`Total from 1000 goroutines: 499500` PASSED，零泄漏
- 独立测试：5000 个空 goroutine 全部完成

#### 验证结果
- 所有测试通过，零泄漏：hello、demo_sort、stress_test、test_array、test_string、test_method、test_null、test_break、test_class

---

## 阶段四：实战验证 + 控制流 + 多类协作
### 日期：2026年4月27日

#### 1. 综合Demo验证
- 编写 `examples/demo_sort.joya`：并发排序
  - 并行 partialSum + chan 汇总 vs 串行 sumArray 验证
  - findMax、bubbleSort、printArray 方法组合测试
  - 输出正确：并行求和=414，排序结果 [11..90]

#### 2. break/continue 控制流
- **Lexer**：新增 `keyword_break`、`keyword_continue`
- **AST**：`Statement.break_stmt`、`Statement.continue_stmt`
- **Parser**：`parseBreakStmt`、`parseContinueStmt`
- **Interpreter**：
  - `error.BreakSignal`、`error.ContinueSignal` 中断循环
  - for_loop、while_loop、for_each 统一捕获：
    - BreakSignal → break 循环
    - ContinueSignal → 跳过当前迭代
    - ReturnSignal → 向上传播
- **测试**：`examples/test_break.joya` 覆盖 while/for/for-each 中的 break 和 continue

#### 3. 多类协作（对象系统）
- **AST**：
  - `Type.class_ref` — 类类型引用（如 `Calculator`）
  - `Expression.new_object` — `new ClassName(args)` 对象创建
  - `Expression.method_call` — `obj.method(args)` 方法调用
  - `Expression.this_ref` — `this` 引用
  - `Statement.this_assign` — `this.field = expr`
  - `Class.fields` — 类字段列表（`FieldDecl`）
- **Lexer**：新增 `keyword_this`
- **Parser**：
  - `parseBaseType` 支持标识符作为类类型
  - `parsePrimary` 支持 `new ClassName(args)`、`this`（带后缀）
  - `parsePostfix` 支持 `obj.method(args)` 方法调用
  - `parseThisAssignOrExpr` 处理 `this.field = expr`
  - `parseClass` 区分字段声明（`type name;`）和方法声明（`type name(`）
  - `parseStatement` 支持类类型变量声明前瞻
- **Interpreter**：
  - `JoyaObject` 结构体：class_name + 字段 HashMap
  - `Value.object` 变体
  - `new_object`：创建对象 → 初始化字段 → 查找构造函数并执行
  - `method_call`：绑定 this → 创建方法环境 → 执行 → 返回值
  - `this_ref`：从环境取 `this`
  - `this_assign`：修改 this 对象的字段
  - `field_access`：支持对象字段读取
  - 构造函数：与方法同名（如 `void Calculator(int v)`）
  - RuntimeCtx 跟踪对象生命周期
- **内存修复**：方法调用中的 `arg_vals` ArrayList 统一使用 defer deinit
- **测试**：`examples/test_class.joya` 覆盖 Counter/Calculator 两个类

#### 验证结果
- 所有8个测试文件通过，无编译错误，无内存泄漏
- `hello.joya`、`features.joya`、`demo_sort.joya`、`test_array.joya`、`test_string.joya`、`test_method.joya`、`test_null.joya`、`test_break.joya`、`test_class.joya`

---

## 阶段三：语言特性充实
### 日期：2026年4月27日

#### 1. 数组支持
- **AST**：新增 `Type.array`（数组类型）、`Expression.new_array`（`new T[size]`）、`Expression.array_literal`（`[1, 2, 3]`）、`Expression.array_index`（`arr[i]`）、`Statement.array_assign`（`arr[i] = expr`）
- **Lexer**：新增 `colon` token（支持 for-each 的 `:` 语法）
- **Parser**：
  - `parseType` 支持 `int[]`、`string[]` 等数组类型（`[]` 后缀）
  - `parseBaseType` 解析基础类型（不含 `[]`），用于 `new T[size]`
  - `parsePrimary` 支持 `new T[size]` 和 `[expr, ...]` 数组字面量
  - `parsePostfix` 处理数组索引 `arr[i]` 和字段访问 `obj.field` 的链式后缀
  - `parseAssignOrExprStmt` 支持 `arr[i] = expr` 索引赋值
  - `parseForLoop` 支持 `for (T x : arr)` for-each 语法（通过前瞻检测 `:` token）
- **Interpreter**：
  - `JoyaArray` 结构体：带缓冲区、元素类型、默认值初始化
  - `Value.array` 变体
  - `RuntimeCtx` 跟踪数组生命周期
  - 数组创建、索引读写、`.length` 属性
  - for-each 循环执行
- **测试**：`examples/test_array.joya` 覆盖所有数组操作

#### 2. 字符串增强
- **索引访问**：`s[0]` 返回单字符字符串（在 `array_index` 求值中支持 string 类型）
- **比较运算符**：`==`、`!=` 支持 string 和 bool 比较
- **`.length` 属性**：字符串和数组均支持
- **float 运算**：`+`、`-`、`*`、`/` 支持 int/float 混合运算
- **字符串拼接**：`+` 支持 null 值的拼接（输出 "null"）
- **测试**：`examples/test_string.joya`

#### 3. 方法参数与返回值
- **方法调用**：在 `eval_expression` 的 `.call` 分支中查找用户定义方法，创建新环境绑定参数，执行方法体
- **返回值**：
  - `RuntimeCtx.return_value` 存储返回值
  - `return_stmt` 设置 `ctx.return_value` 并抛出 `error.ReturnSignal` 中断调用链
  - 方法调用方捕获 `ReturnSignal`，取回返回值
  - `run()` 中 `main()` 的执行也捕获 `ReturnSignal`
- **测试**：`examples/test_method.joya` 覆盖无参/有参/返回值/嵌套调用/数组参数/字符串返回

#### 4. null 值支持
- **Lexer**：新增 `keyword_null`
- **AST**：`Expression.null_literal`
- **Interpreter**：`Value.null_val` 变体
- **通道 receive**：关闭后返回 `null_val`（而非 `void`）
- **比较运算符**：`==`/`!=` 支持 null 比较
- **字符串拼接**：null 拼接为 "null"
- **printValue**：null 输出 "null"
- **测试**：`examples/test_null.joya`

#### 验证结果
- 所有测试通过：`hello.joya`、`features.joya`、`test_array.joya`、`test_string.joya`、`test_method.joya`、`test_null.joya`
- 无编译错误

---

## 阶段二：递归下降解析器实现
### 日期：2026年4月23日

#### 重大进展：parser.zig 完全重写为递归下降解析器
1. **移除硬编码AST**：不再按照hello.joya的固定token序列逐一验证，改为通用的递归下降解析
2. **完整的表达式解析**（优先级爬升法）：
   - `parseComparison`：解析 < > <= >= == !=
   - `parseAddition`：解析 + -
   - `parseMultiplication`：解析 * /
   - `parseUnary`：解析 一元减号
   - `parsePrimary`：解析 整数字面量、浮点字面量、字符串字面量、布尔字面量、标识符、new chan、内置函数调用、括号表达式
3. **完整的语句解析**：
   - `parseBlock`：解析 { ... } 块
   - `parseVarDecl`：解析类型 变量名 = 表达式;
   - `parseAssignOrExprStmt`：解析赋值、++/--、函数调用、表达式语句
   - `parseGoStmt`：解析 go { ... };
   - `parseForLoop`：解析 for (init; cond; inc) { ... }，支持i++/i--/i=expr
   - `parseIfStmt`：解析 if/else if/else
   - `parseReturnStmt`：解析 return;
   - `parsePrintStmt`：解析 print/println
4. **方法解析**：`parseMethod` - 跳过public/static修饰符，解析返回类型、方法名、参数列表、方法体
5. **类解析**：`parseClass` - 解析类名、方法列表
6. **程序解析**：`parse` - 解析多个类，构建完整AST

#### main.zig 更新
- 移除硬编码提示信息
- 精简输出：仅显示 token 数量、类数量

#### 修复的编译错误
- Zig 变量遮蔽：`init` → `init_expr`、`loop_init`；`else_branch` → `else_br`

#### 验证结果
- `zig build` 编译成功
- `zig-out\bin\joya.exe examples\hello.joya` 输出正确：
  ```
  sent: 0
  received: 0
  sent: 10
  received: 10
  sent: 20
  received: 20
  ```
- 内存泄漏为原型阶段预期（Program.deinit为空操作），后续处理

---

## 阶段一：解释器原型开发
### 日期：2026年4月21日

#### 初始实现
1. 完整的词法分析器（lexer.zig）
2. 抽象语法树结构（ast.zig）
3. 解释器核心（interpreter.zig）- Go风格并发、Chan通道、OS线程
4. 入口文件（main.zig）
5. 硬编码式parser，验证hello.joya的token序列后返回手写AST

---

## 使用说明
- 编译：`zig build`
- 运行：`zig-out\bin\joya.exe examples\hello.joya`
