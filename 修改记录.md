# Joya语言开发修改记录

## 阶段三：语言特性充实
### 日期：2026年4月27日

#### 1. 数组支持
- **AST**：新增 `Type.array`（数组类型）、`Expression.new_array`（`new T[size]`）、`Expression.array_literal`（`[1, 2, 3]`）、`Expression.array_index`（`arr[i]`）、`Statement.array_assign`（`arr[i] = expr`）
- **Lexer**：新增 `colon` token（支持 for-each 的 `:` 语法）
- **Parser**：
  - `parseType` 支持 `int[]`、`string[]` 等数组类型（`[]` 后缀）
  - `parseBaseType` 解析基础类型（不含 `[]`），用于 `new T[size]`
  - `parsePrimary` 支持 `new T[size]` 和 `[expr, ...]` 数组字面量
  - `parsePostfix` 处理数组索引 `arr[i]` 和字段访问 `obj.field` 的链式后缀
  - `parseAssignOrExprStmt` 支持 `arr[i] = expr` 索引赋值
  - `parseForLoop` 支持 `for (T x : arr)` for-each 语法（通过前瞻检测 `:` token）
- **Interpreter**：
  - `JoyaArray` 结构体：带缓冲区、元素类型、默认值初始化
  - `Value.array` 变体
  - `RuntimeCtx` 跟踪数组生命周期
  - 数组创建、索引读写、`.length` 属性
  - for-each 循环执行
- **测试**：`examples/test_array.joya` 覆盖所有数组操作

#### 2. 字符串增强
- **索引访问**：`s[0]` 返回单字符字符串（在 `array_index` 求值中支持 string 类型）
- **比较运算符**：`==`、`!=` 支持 string 和 bool 比较
- **`.length` 属性**：字符串和数组均支持
- **float 运算**：`+`、`-`、`*`、`/` 支持 int/float 混合运算
- **字符串拼接**：`+` 支持 null 值的拼接（输出 "null"）
- **测试**：`examples/test_string.joya`

#### 3. 方法参数与返回值
- **方法调用**：在 `eval_expression` 的 `.call` 分支中查找用户定义方法，创建新环境绑定参数，执行方法体
- **返回值**：
  - `RuntimeCtx.return_value` 存储返回值
  - `return_stmt` 设置 `ctx.return_value` 并抛出 `error.ReturnSignal` 中断调用链
  - 方法调用方捕获 `ReturnSignal`，取回返回值
  - `run()` 中 `main()` 的执行也捕获 `ReturnSignal`
- **测试**：`examples/test_method.joya` 覆盖无参/有参/返回值/嵌套调用/数组参数/字符串返回

#### 4. null 值支持
- **Lexer**：新增 `keyword_null`
- **AST**：`Expression.null_literal`
- **Interpreter**：`Value.null_val` 变体
- **通道 receive**：关闭后返回 `null_val`（而非 `void`）
- **比较运算符**：`==`/`!=` 支持 null 比较
- **字符串拼接**：null 拼接为 "null"
- **printValue**：null 输出 "null"
- **测试**：`examples/test_null.joya`

#### 验证结果
- 所有测试通过：`hello.joya`、`features.joya`、`test_array.joya`、`test_string.joya`、`test_method.joya`、`test_null.joya`
- 无编译错误

---

## 阶段二：递归下降解析器实现
### 日期：2026年4月23日

#### 重大进展：parser.zig 完全重写为递归下降解析器
1. **移除硬编码AST**：不再按照hello.joya的固定token序列逐一验证，改为通用的递归下降解析
2. **完整的表达式解析**（优先级爬升法）：
   - `parseComparison`：解析 < > <= >= == !=
   - `parseAddition`：解析 + -
   - `parseMultiplication`：解析 * /
   - `parseUnary`：解析 一元减号
   - `parsePrimary`：解析 整数字面量、浮点字面量、字符串字面量、布尔字面量、标识符、new chan、内置函数调用、括号表达式
3. **完整的语句解析**：
   - `parseBlock`：解析 { ... } 块
   - `parseVarDecl`：解析类型 变量名 = 表达式;
   - `parseAssignOrExprStmt`：解析赋值、++/--、函数调用、表达式语句
   - `parseGoStmt`：解析 go { ... };
   - `parseForLoop`：解析 for (init; cond; inc) { ... }，支持i++/i--/i=expr
   - `parseIfStmt`：解析 if/else if/else
   - `parseReturnStmt`：解析 return;
   - `parsePrintStmt`：解析 print/println
4. **方法解析**：`parseMethod` - 跳过public/static修饰符，解析返回类型、方法名、参数列表、方法体
5. **类解析**：`parseClass` - 解析类名、方法列表
6. **程序解析**：`parse` - 解析多个类，构建完整AST

#### main.zig 更新
- 移除硬编码提示信息
- 精简输出：仅显示 token 数量、类数量

#### 修复的编译错误
- Zig 变量遮蔽：`init` → `init_expr`、`loop_init`；`else_branch` → `else_br`

#### 验证结果
- `zig build` 编译成功
- `zig-out\bin\joya.exe examples\hello.joya` 输出正确：
  ```
  sent: 0
  received: 0
  sent: 10
  received: 10
  sent: 20
  received: 20
  ```
- 内存泄漏为原型阶段预期（Program.deinit为空操作），后续处理

---

## 阶段一：解释器原型开发
### 日期：2026年4月21日

#### 初始实现
1. 完整的词法分析器（lexer.zig）
2. 抽象语法树结构（ast.zig）
3. 解释器核心（interpreter.zig）- Go风格并发、Chan通道、OS线程
4. 入口文件（main.zig）
5. 硬编码式parser，验证hello.joya的token序列后返回手写AST

---

## 使用说明
- 编译：`zig build`
- 运行：`zig-out\bin\joya.exe examples\hello.joya`
