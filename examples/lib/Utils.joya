class Utils {
    static void log(string msg) {
        println("[LOG] " + msg);
    }

    static string repeat(string s, int n) {
        string result = "";
        for (int i = 0; i < n; i++) {
            result += s;
        }
        return result;
    }
}
